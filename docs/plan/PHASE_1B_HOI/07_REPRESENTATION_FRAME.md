# Phase 1B-07：表示帧缺陷修复与基线重建（P12）

本文件于 2026-08-19 新建，用于记录发布代码的坐标表示帧缺陷在 `phase/01b-hoi` 上的修复
与随之而来的基线重建。缺陷由 `phase/01c-hsi` 于 2026-08-18 定位（提交 `3ded4eb`），
交接文档 `/data/yujinlun/report/蔚金伦_260818_坐标表示缺陷修复与HOIPrior重训交接.md`；
本文件记录的是**在 OMOMO 上独立复测后的 HOI 侧结论**，其中若干条与交接文档正文相反，
每一条都附实测数字。
导航：[总览](../OVERVIEW.md) · [Phase 1B 索引](README.md)

#### 2026-08-19 Phase 1B P12 表示帧缺陷修复与基线重建（用户批准）

### 口径声明：这不是一个单因子实验

本仓惯例是"一个实验只动一个因子"。**P12 不满足该惯例，且不应被当作满足。**
它是一次**发布代码的缺陷修复 + 基线重建**：修复项按构造捆绑，实测任何真子集都比现状更糟
（见"为什么必须一次打完"），因此无法拆成单因子序列。预注册在此明确该偏离，
并把可归因的科学内容收缩到一个**同 checkpoint 内**的对照上（见门控 (ii)）。

| 项 | 固定值 |
|---|---|
| 子阶段 | `1B-P12` |
| 训练 run id（拟） | `p1-hoi-p12-frame-repair-baseline-s42-20260819` |
| 臂配置 | `code/config/config_train_hoi_prior_p12.yaml`——**只含配方组合与 run id，无任何被操纵因子** |
| 配置组合 | `defaults: [config_train_hoi_prior, recipe: d2ai, _self_]`；`hand_object_contact_weight` 保持 0.0 默认（即 D2-AI 的目标函数，非 W3/P10/P11 的） |
| 评测 run id（拟） | 由 `tools/hoi_chain.py` 派生，主 cell 与对照 cell 各一 |
| 主机 | worker `node01` / `infbagel-4gpu`（10.181.9.214），4× RTX 3090 |
| 载体 | `tools/hoi_chain.py` 链式 train → evaluate → bootstrap，只回传结果 |
| 预算 | 299,520,000 窗口 = 146,250 次更新（= D2-AI 全预算，未改） |
| 有效 batch | 2048 = micro 512 × 4 GPU × accum 1（已注册常规档） |
| LR / warmup / clip | 1e-4 / 无 / 无（沿用 `recipe/d2ai.yaml`，冻结未改） |
| seed / 初始化 | 42 / 随机（不从发布 checkpoint 初始化） |
| 参考成本 | D2-AI 实测 21.74 h @ 3827 windows·s⁻¹ |

### 单一变更：表示帧修复（6 文件，一次打完）

| 文件 | 变更 | 路径归属 |
|---|---|---|
| `code/priors/hoi/data.py` | 世界校正只左乘 root；模板反共轭回 y-up；不再共轭 `human_pose` | **训练** |
| `code/priors/core/window_codec.py` | heading 由内旋 `"ZXY"[…,2]` 改为外旋等价的 `"YXZ"[…,0]` | 训练 + 评测 |
| `code/datasets/utils.py` | 新增按语料功能性判定世界系的探针与校正工具 | 共同上游 |
| `code/datasets/infbagel.py` | 同上约定；`transl` y-up 恢复改为**逐布局** | **评测** |
| `code/test_infbagel_hoi.py` | 删除 `:186-191` 的补偿性 SMPL-X 三明治 | 评测 |
| `code/utils.py` | `interpolate_joints` 移到 `interp_jrot` 的 `1/scale` 基准 | 评测 |
| `tests/core/test_contract_freeze.py` | 冻结哈希重钉 `74ed3353…` → `d545359b…` | 治理 |
| `code/test_infbagel_hosi.py` | 与 HSI 分支 parity（本分支零 importer） | 治理 |

`code/config/recipe/d2ai.yaml` **逐字节未改**。`fk_weight` 保持 `0.3569973401779424`。

### 为什么必须一次打完（实测，不是判断）

| 部分修复 | 后果 |
|---|---|
| 只打 `window_codec.py` | heading 误差 137.94° → 137.36°，约定错误单独只值 **0.623°**，近似 no-op |
| 只打评测侧（不动 `priors/hoi/data.py`） | 制造一个今天不存在的约 82° 训练/评测帧错配 |
| 删 `transl` 恢复 + 删三明治 | pelvis 绝对位置误差 0.0001 m → **0.5378 m** |
| 保留三明治 + 修源头 | 顶点移 **1.87 m**，不崩，但 SDF 查询出盒、`hand_pen`/`human_pen` 静默归零 |
| 修旋转但不反共轭模板 | `data/test` 上 FK 闭合 7.06e-07 m → **1.111 m** |

### 预期收益：采样效率假设 + 评测一致性

**不主张"修好了 HOIPrior 迭代无效的根因"。** 该主张在 OMOMO 上已被测量否证：
FK 对 `human_joints_aligned.npy` 的误差修复前 **2.80e-07 m** / 修复后 2.83e-07 m（3000 帧 / 60 序列，
两个消融分别失败于 0.371 m 与 0.601 m）；`losses["fk"]` 在 GT 窗口上 1.10e-13 → 1.07e-13。
交接报告的 0.56 m 位移是 **LINGO 独有**，OMOMO 上共轭逐级相消
（`G_yup·Mᵀ · (M Pᵢ Mᵀ)… · M o_true = G_yup·P_chain·o_true`）。

**(A) 采样效率**（训练侧，OMOMO `data/train`，训练路径，1200 窗口 seed 42）：

| 量 | 完全不正则化 | 修复前 | 修复后 |
|---|---|---|---|
| 髋方位角对圆均值 p50 | 22.04° | **46.28°** | **2.13°**（p95 3.94） |
| 被去掉的 \|shift\| | — | 93.19° / p50 90.40 | 42.206 / p50 22.755（== 真实 heading，偏差 ≤1.03e-05°） |
| 旋转通道（132 维）总方差 | — | 24.399 | **11.539**（2.11×） |
| 关节通道（84 维）总方差 | — | 1.7097 | 1.5432 |
| 根 6-D 首帧总方差 | — | 0.8942 | **0.0579**（15.4×） |
| 根 6-D 首帧测地散布 | — | mean 63.40° / p50 46.93 / max 179.87 | **mean 11.12° / p50 9.67 / max 46.02** |

（1200 窗口 seed 42；总方差定义 = 逐维方差在 pooled `n_windows×16` 帧上求和，无偏。
400 窗口口径给出 24.294 → 11.398 与 0.8881 → 0.0543，两个口径一致。
修复后根 6-D 逐维 std `[.0066,.075,0.0,.0722,.0377,.2134]`——第三维**恰好为 0**，
即 heading 自由度被正则化收掉，这是该量应有的结构性签名。）

**2×2 分解（根 6-D 首帧，1200 窗口 seed 42）——本臂最硬的一条实测：**

| 约定 × 输入 | 总方差 | 测地散布 mean / p50 / max |
|---|---|---|
| 旧约定 `ZXY[2]` × 共轭输入（= 修复前） | 0.8942 | 63.40° / 46.93 / 179.87 |
| 新约定 `YXZ[0]` × y-up 输入（= 修复后） | **0.0579** | 11.12° / 9.67 / 46.02 |
| **旧**约定 × y-up 输入（只修输入） | 0.0577 | 11.13° / 9.68 / 46.76 |
| **新**约定 × 共轭输入（只修 codec） | **1.0364** | **79.85°** / 57.36 / 179.82 |

结论：收益几乎全部来自**修输入**（0.0577 ≈ 0.0579）；而**只修 `window_codec.py`
比什么都不做更糟**——方差 0.8942 → 1.0364（+16%），散布 63.40° → 79.85°（+26%），
同时作废全部已封存 checkpoint。交接报告第 6 节第 1 条建议"先把 `window_codec.py`
的同款改动落到 `phase/01b-hoi`"，按此实测**不成立**：它不是无害的第一步，是有害的第一步。

旧代码不是"未做 heading 正则化"，而是**在注入朝向噪声**（22.04 → 46.28），修复后 2.13，
优于完全不做。这是本臂唯一可主张的训练侧机制，且它是**假设**而非已证结论：
"更集中的窗口流形提升样本效率"未在 HOI 上直接测量过。

**(B) 评测一致性**（`data/test`，438 个评测窗口）：训练走 codec，评测 step ≥1 也走 codec
故本来精确一致（0.000°）；**step 0 走 `datasets/infbagel.py:439-457`，修复前错配 50.12° 均值 /
179.89° 最大，修复后约 1e-06°**。符号是**条件化**而非错误解码（`mat` 在 encode/decode 对称使用），
故修复前模型显得**比实际更差**。`obj_rot` 帧不变，故坏掉的是人-物朝向**配对**。

### 比较对象：没有可比的 before，本臂重建基线

所有 D2-* 模型侧几何行**与** released 基线行（`p0-hoi-table5-baseline-s42-20260712`）
同时被表示变更与评测器变更作废，且**不可重算**：旧 checkpoint 拟合的是 `G_yup·Mᵀ` 通道，
再叠加一个逐窗口、且是该窗口自身 root 的函数的 y 旋转，在输出上不可逆。
**禁止**报一个跨越该边界的 before/after 差值。

可归因的科学内容收缩为**同 checkpoint 内**的两个评测 cell：

| cell | step-0 帧规则 |
|---|---|
| **PRIMARY** | 修复后的规则（外旋 y，与训练一致） |
| **CONTROL** | **复算历史 step-0 帧规则**：把已修好的 root 临时重新共轭，再取 scipy 外旋 yaw 建帧 |

对照 cell **不采用**"翻回 codec 约定"：实测约定错误单独只值 0.623°，是 no-op，
会把全部差异记到模型上。成本：多一趟 438 序列 unguided。

### 数据层前置闸门（已全部通过，CPU，非结果门控）

| 闸门 | 结果 |
|---|---|
| 世界系探针 | `data/train`→'z'、`data/test`→'z'、`data/dataset`→'y'；两假设残差差 6 个数量级 |
| A 通道竖直 vs 关节竖直 | 修复后 22.5412357 **== 原始资产** 22.5412352；修复前 68.6050731 **== 被共轭的原始资产** 68.6050723（各 7 位）；FK 闭合两侧同为 2.6245e-07 m |
| B heading | 见上表；shift 前后直立度逐位不变 |
| E 旧 dataset vs `core/` codec | 旋转测地 50.12° → 2e-06°；关节通道 0.943 → 1.79e-07 |
| 四格帧 | step 0 50.12° → 9.75e-07°；step ≥1 两侧精确 0.000° |
| 绝对位置 | pelvis 0.1347 mm；对 GT 的绝对精度改前改后不变到 2.6e-08 m；pre→post 为刚体平移（0.0008 mm） |
| 插值 | 新网格与 `interp_jrot` 逐位相同；`trans_dist` 伪影 −1.23 cm；GT FS 0.35632 → 0.31654；**MPJPE Δ 恰好 0.000e+00** |
| 全仓测试 | 320 passed（默认）／317 passed 3 skipped（`INFBAGEL_WORKER_EXPERT=hoi`），与改前完全一致 |

### 结果门控（在看到结果之前写死）

- **(i) 运行与体检**：训练稳定完成 146,250 次更新，损失有限；checkpoint 通过体检四量
  （阈值见 `docs/plan/PHASE_1B_HOI/` 同节体检小节，源自 OMOMO 真值，**不得沿用 HSI/LINGO 的数**）。
  体检不通过则不花完整评测预算。
- **(ii) PRIMARY——评测一致性**：PRIMARY cell 必须在 `trans_dist` 与 `end_obj_trans_err` 上
  **显著优于** CONTROL cell，且 `contact_f1` 不得显著更差。这是本臂唯一的对照式判据。
- **(iii) 真值地板**：438 协议的 GT 参照行必须先产出并封存为 HOI 常驻真值参照行。
  **若 `foot_sliding` 的 GT 地板 ≥ released 行的 0.33336，则 FS 降级为诊断量、不作门控指标**
  （已知 GT FS 在插值修复后为 0.31654，需以完整 438 协议确认）。
- **(iv) 基线交付**：产出含全部预注册指标的新基线行，供 P13+ 比较。

### 停止分类

- `frame-repair-baseline-established`：(i)(iii)(iv) 成立，且 (ii) 成立；
- `eval-consistency-null`：(ii) 失败——step-0 帧一致性对指标无实质贡献，
  则"评测一致性"这一半收益判负，采样效率假设仍未被检验，下一入口是训练侧对照而非评测侧；
- `health-check-fail-stop`：(i) 的体检不通过——表示修复未能产出可用 checkpoint，
  回到数据层排查，不进入评测预算；
- `baseline-unstable-stop`：训练未稳定完成——按操作性失败保留，不复用 run id。

### 文件范围（锁定）

上表 8 个文件，加 `tests/hoi/` 下一个新增表示帧回归测试，加 `config_train_hoi_prior_p12.yaml`
（`tools/hoi_chain.py:70` 要求可上报的臂自己声明 run id，故该 fragment 必需；它不含任何被操纵因子），
加本节与一行 registry。**不改 `recipe/d2ai.yaml`，不改任何既有测试或 validator，
不改 `experiments/training_resource_protocol.json`。**

### 已知局限（预先声明，不等结果出来再解释）

1. **heading 正则化对水平身体退化，且 OMOMO 比 LINGO 严重。** OMOMO 躯干倾角
   p50 11.5° / p75 **51.5°** / p95 76.4° / max 94.1°。修复后髋方位角 p50 2.13 / p95 3.94 说明主体良好，
   但任何单轴 heading 对接近水平的躯干都退化。闸门 A 修复后的 22.54° 大于报告在 LINGO 上的 8.98°，
   **是语料差异**（它精确等于原始资产本身），不得当作修复不到位。
2. **骨长刚性必须在粗帧上算。** 粗帧 max 3.6e-06、0 根超 1e-2；经 `interp_s=3` 插值后
   max 0.036、**120 根超 1e-2**。在导出的插值 motion 上跑这条会把完好真值判坏。
3. **`fk_weight` 未重新推导。** 它是梯度范数均衡的产物（`auxiliary_balancing.py:57-62`，
   target 0.6279429736100133 / raw_fk 1.7589570087469566），不是损失量级校准。
   保持不动的依据是匹配扰动（σ=0.02/0.05/0.10）下的敏感度比 **1.021**——
   **1.021 是敏感度比，是梯度范数均衡的代理量，不是重新推导。** 忠实的重新推导需要在新表示下
   从随机初始化重测逐场梯度范数，属另一个预注册诊断。
4. **`code/priors/hsi/data.py` 在本分支仍共轭 root 与 pose**，两个专家的 dataset 因此暂不一致。
   归 `phase/01c-hsi` 所有；`core/` 已逐字节一致（`d545359b…`），graft 前提未破。
5. 四处 `window_codec.py:251→:260` 与 `hoi/data.py:97/200/206` 的行号引用过时，
   其中两处在 append-only 记录里不可更正。codec 必须与 `3ded4eb` 逐字节相同，行移不可避免。

### 不做什么

- **不调整几何项权重。** 交接报告正文建议下调，附录 A.1 已自我订正为"保持"；
  在 HOI 上更强：`losses["fk"]` 本来就在精确极小点（1.10e-13），无标度变化可言。
- **不新增臂配置或新配方。** 本臂就是 D2-AI 的配方在修复后的表示上重跑。
- **不维护 legacy 表示代码路径。** 旧行作为"修复前"留档；为拿到表示侧的 before/after
  需要同时翻回 step-0 帧、插值基准与三明治三个开关，那等于维护死代码。
- **不为了让检查通过而改测试或 validator。** 唯一的测试改动是冻结哈希重钉，
  那是冻结契约自身规定的授权变更程序。

---

## 实现追记：CONTROL 对照 cell（2026-08-20，用户批准）

追加节，不改上文。上文 `### 已知局限` 第 5 条与 `### 不做什么` 末条仍然成立：本节的开关
**只翻 step-0 帧一个开关**，不翻插值基准与三明治，所以它不是"legacy 表示代码路径"，
而是一个评测侧的诊断 cell。

### 机制

`InfBaGelDataset` 新增构造参数 `step0_frame_rule`，取值 `repaired`（默认）|
`historical_conjugated`，经 `+dataset.step0_frame_rule=…` 传入，与 `asset_world_up` 同路径。
**故意不写进 `config/dataset/omomo_test.yaml`**：那会在每个 PRIMARY 的 resolved config 里
多出一行；已实测 PRIMARY 的 resolved config 逐字节不变。

CONTROL 下**只有 `init_global_orient_euler` 的来源变了**：历史 root 由修复后的 root 代数重建为
`M C^T R_rep M^T`（OMOMO 上化简为 `R_rep M^T`），再取 scipy 外旋 `'zxy'[2]`。
`shift_euler`、`shift_rot_matrix`、`mat`、`joints @ S.T` 四处**是发布版原文，未改一字**。
所以两个 cell 是**同一份数据集的两个函数**，差别只在窗口帧，别无其他。

### 关键发现：CONTROL 打开的帧误差是它要复现的缺陷的 1.55 倍

438 个 step-0 窗口实测（deg）：

| 量 | 含义 | mean | p50 | p95 | max | >5° |
|---|---|--:|--:|--:|--:|--:|
| `\|C−P\|` | 只翻 codec 约定（已否决路线） | **0.410** | 0.128 | 1.79 | 4.62 | 0.0% |
| `\|A−B\|` | **发布版真实的训练/评测错配** | **50.12** | 38.98 | 145.14 | 179.89 | 90.9% |
| `\|A−P\|` | **本 CONTROL 实际打开的** | **77.83** | 51.89 | 175.60 | 179.81 | 91.6% |

（P = `C R_stored` 的外旋 `'zxy'[2]`；A = `M R_stored M^T` 的同一读法；B = 同输入的
pytorch3d 内旋 `"ZXY"[…,2]`；C = `C R_stored` 的内旋读法。）

`|A−B|` 复现上文 `:102` 记录的 50.12° 到 4 位有效数字（50.122），这是"A 确为发布版评测规则"
的最强可得识别，并已写成测试断言。已否决的 codec-flip 路线实测 0.410°，确认是 no-op，
CONTROL 是它的 **190 倍**。

**这改变 gate (ii) 的读法，必须在解释结果前声明。** 50.12° 是**同一输入上的两种约定**，
而 CONTROL 是**同一约定作用在两种输入上**，量的结构不同，所以过冲到 1.55 倍。
后果：在 77.83° 平均帧误差、52% 窗口过 45° 之下，PRIMARY 打赢 CONTROL 接近必然，
`eval-consistency-null` 从"有信息量"变成"会很意外"。

**它仍然可用，但只作上界。** D2-AI 的条件是"评测用 A、模型训练用 B"（错配 50.12°），
CONTROL 是"评测用 A、模型训练用修正规则"（错配 77.83°）——结构同类、错配更大，
故 CONTROL 给出评测器贡献份额的**上界**，`77.83 ≥ 50.12` 正是上界成立的理由。
**忠实的 `A` vs `B` 用本 checkpoint 做不出来**：它要求模型在 `B` 下训练，而 P12 训练在修正规则下。
这不是 `core/` 改动能解决的问题。

### 安全性证据（默认路径逐位不变）

两条互相独立的测量：

1. 编辑前在干净 HEAD `2593162` 上导出全部 **1314 个窗口**的 14 个数组
   （`mat, global_rot_6d, global_rot_6d_gt, joints, joints_gt, pelvis_goal, scene_goal,
   object_goal, object_trans, object_rot_mat, obj_rot_mat_ref, rest_human_offsets, transl,
   starts`），编辑后不带覆盖、以及显式 `=repaired` 各导一次：**14/14 逐字节相同，
   `maxabsdiff` 恰为 0.000e+00**。
2. 针对"可能导错了键"这一残余风险：还原 HEAD 的两个源文件，把 `__getitem__` 的**全部 35 个键**
   在 64 个窗口上逐键 SHA256，再换回：**35/35 哈希相同，0 键有别**。

`code/datasets/utils.py` 是**纯追加**（`current[:221]` 与 HEAD 逐字节相同，追加 46 行），
这一性质是承重的：训练用的 `code/priors/hoi/data.py:16` 从 `datasets.utils` import。
`code/train_hoi_prior.py` 不 import `datasets.infbagel`，已写成测试断言。
8 处对抗式源码回退（默认翻转、丢 `C^T`、CONTROL 换成 codec-flip、丢 shift 符号、
改用内旋 `'ZXY'`、删分支、`mat` 与通道脱同步、euler 索引取 0）**全部被测试捕获**。
其中"删分支"是被 (b) 的 repaired 半边捕获，**不是**被逐位测试捕获——逐位测试比较两个实例，
删分支会让两者一起移动。该局限已写进测试 docstring 并指明绝对锚点。

**GT 参照行可证明不动**：`joints_gt`、`object_rot_mat`、`obj_rot_mat_ref`、
`rest_human_offsets`、`transl` 在两个 cell 间逐字节相同，绝对全局关节一致到 **7.57e-07 m**，
且各自复现 `human_joints_aligned.npy` 到 3.97e-07 / 4.06e-07 m。

测试：`pytest tests` **340 passed**（HEAD 为 334，新增 6）；
`INFBAGEL_WORKER_EXPERT=hoi` **337 passed / 3 skipped**；零失败。
评测侧开销：1314 窗口含构造 9.39 s（repaired）vs 9.11 s（historical），无可测差异。

### 启动路线：绕开 hoi_chain 的阶段守卫

`tools/hoi_chain.py` 的 `stage_completed` 按**阶段名**取状态文件
（`results/experiments/<train_run_id>/chain/evaluate.json`），**不按 eval run id**，
所以第二次 `--stages evaluate` 会打印 "evaluate already completed; not rerunning" 而静默跳过。
CONTROL 因此**直接调用评测器**，命令与 `hoi_chain.evaluate_command` 构造的逐字节相同，
只多一个 `+dataset.step0_frame_rule=historical_conjugated` 覆盖。
不改 `tools/hoi_chain.py`——把状态文件改按 eval run id 取键是可行的修法，但那会动到
所有历史臂共用的编排代码，超出本次范围。

### 行号锚点订正

上文 `:102` 引 `datasets/infbagel.py:439-457`，该处代码现在在 `:564-574`。
本次插入使其再下移 20 行；此前已过时。append-only 记录，不原地更正。

---

## 结果订正：门控 (iii) 里的 GT `foot_sliding` 是 0.26346，不是 0.31654（2026-08-20）

追加节，不原地改上文门控行。**若与上文冲突，以本节为准。**

上文门控 (iii) 的括注写"已知 GT FS 在插值修复后为 0.31654"。**这是一处归类错误，是我写的。**

`foot_sliding` **不经过** `interpolate_joints`。代码链：GT 走 `test_infbagel_hoi.py` 的
`data_dict['joints_gt'] = self.joints[start:end]`（原生 48 帧）加未 `[::step]` 的
`global_rot_6d_gt`，直接 FK 成 `points_fk_all_gt_48`；`interpolate_joints` 只作用于**模型**的
16 帧输出。**真值行与插值修复正交。**

0.31654 的真实来处是早期评测审计里三个**并列**的数：
`shipped 0.35632 / desync-corrected 0.31654 / un-interpolated GT 0.24808`，
我把中间那个标成了 "GT FS" 并写进门控。

数值反证：预测 root 在插值三元组内的二阶差为 **2.4e-07 m**（float32 噪声，证明它就是 1/scale
线性插值），GT root 同一量为 **7.8e-03 m**（证明未插值）。

### 438 协议实测的真值参照行（封存）

| | GT 地板 | P12 模型行 | |
|---|--:|--:|--:|
| `foot_sliding` | **0.26346464114890705**（p50 0.24276 / p95 0.56484） | 0.34159 | **1.30×** |
| `feet_height` | **0.034326739609241486** | 0.06110 | +2.68 cm |
| `gt_contact_percent` | **0.6618830180474017** | 同值 | 跨 47 个 438 序列 run 逐位相同 |

探针正确性：同一脚本对 predictions 复算得 `0.3415924303475854` / `0.06109542399644852`，
与 `aggregate_metrics.json` **逐位相同**。

12 个指标解析已定（GT 自比恒为 0 的六项、接触四项恒为 1.0、`contact_percent` 恒等于
`gt_contact_percent`）；**4 个穿透指标未测**——需 SMPL-X 顶点与物体 rest SDF，导出 npz 只有
`global_jpos`。HOI 侧**没有**现成的真值评测通路：`save_chois_eval_npz` 只导出关节给 CHOIS 的
FID/R-Precision 评测器，不参与那 18 个原生指标。

**门控后果与误标所暗示的相反**：GT 地板 0.26346 **低于** released 行的 0.33336，
故 `foot_sliding` **不降级**，仍是门控指标。

### 两处执行偏离，如实记录

1. 门控 (iii) 写的是 GT 参照行须**先**产出；实际是 PRIMARY 评测先跑（chain 自动执行），
   GT 行随后建立。无科学后果：GT 行对 step-0 帧规则可证明不变（两 cell 间 7.57e-07 m），
   且它是对未变数据资产的纯 CPU 复算。
2. 若把真值行做成"config 开关在模型输出层替换 GT"，会**必然重现 0.31654 那个错数**，
   因为 `compute_metrics` 会对被替换的 16 帧再跑一遍插值。故本行由只读探针在 post-FK 层复算。

### 三条必须随行的口径限制

1. 两行不在同一平滑度基线上（模型 126 帧是 3 帧线性/slerp 段，GT 原生 30 Hz）。弦长插值只会
   **低估**滑动，故不是偏袒 GT；但不能靠"给 GT 也插值"消除，那会污染真值。
2. `feet_height` 是 DBSCAN 从运动自身估的地板高度、非绝对地面，2.68 cm 是垂直偏置的诊断量。
3. `compute_foot_sliding_for_smpl` 原地修改输入且缺权重钳制（低于地板权重可达 2.0），
   故 FS 列部分在测穿透；对两行同向。

---

## 结果订正：真值参照行的接触指标只有 `contact_acc` 是 1.0，另三项上限 0.906392694063927（2026-08-21）

追加节，不原地改上文。**若与上文冲突，以本节为准。**
封存的紧凑结果 `experiments/results/p1_hoi_p12_frame_repair_baseline_s42_20260820.json`
**保持逐字节不变**（sha256 `08bae281fa15…` 被 P12 完成行与 phase summary 第 6、240 行钉住，
按 `docs/EXPERIMENT_CONVENTIONS.md` §5 封存结果只追加不重写），其中的错值 1.0 原样留存。
详见 `docs/HOIPRIOR_EVIDENCE_INDEX.md` 结论 20 与
`docs/phase_summaries/PHASE_1B_P12_REPRESENTATION_FRAME.md` 2026-08-21 追记。

上文 `:314` 写"接触四项恒为 1.0"。**这是错的，是我写的。** 正确的是：

| 指标 | 上文写的（错） | 正确值 |
|---|--:|--:|
| `contact_precision` / `contact_recall` / `contact_f1` | 1.0 | **0.906392694063927** = 397/438 |
| `contact_acc` | 1.0 | **1.0（正确，不订正）** |

成因：438 条官方测试序列里有 **41 条没有任何真值接触帧**，而
`code/eval_metrics.py:311-323` 在 `TP + FP == 0` / `TP + FN == 0` 时把 precision / recall
直接置 0（两者皆 0 时 f1 也置 0），**真值自比也不例外**——那时 `FP = FN = 0`、
`TP = gt_contact_cnt = 0`。`code/test_infbagel_hoi.py:340` 按逐序列均值聚合，
故这 41 条以精确 0 进入**任何模型**的这三列，可达上限就是 397/438。
`contact_acc` 不受影响（零接触序列 `TP = FP = FN = 0`、`TN = 126`，准确率确实是 1.0）；
`contact_percent` 恒等于 `gt_contact_percent` = 0.6618830180474017 也仍然正确。
"12 个指标解析已定"这个计数不变，只有其中三项的值变了。

证据：这 41 条不是阈值边缘个案——最近的一条最小手-物距离 **0.07432 m**（比 0.05 m 阈值还远
2.43 cm），最远 **0.62411 m**，全 438 条里只有 **1** 条落在阈值 1 cm 以内。用评测器自己的规则
（手关节 22/23、0.05 m、126 帧跨度）从数据集加 rest 物体网格独立复算，计数逐位复现，
平均 `gt_contact_percent` 与封存值差 **0.0**；更紧的跨模型独立上界——全部 47 个已封存 438 序列 run 的
"三项同时精确为 0"集合取交集（24 个互异集合）——给出 **42**，只比 41 多 `sub17_woodchair_029`
（126 帧里 1 帧真值接触、最小距离 0.04549 m，全协议唯一的阈值边缘序列，从无模型命中）。只在有定义的 397 条上做真值往返参照得
precision **0.9968411711880822** / recall **0.9954630506006943** / f1 **0.9960322331956971**。

连带覆盖率事实：四个穿透指标只在 **181/438** 条上计算（`code/test_infbagel_hoi.py:276` 硬编码排除
woodchair、whitechair、largebox、largetable、plasticbox、trashcan），而**这 41 条里有 32 条同时被
穿透排除**——既无接触读数也无穿透读数，却仍占 438 的分母；只有 9 条保留穿透读数。

后果：本仓库任何把这三列读作"离 1.0 还差多少"的说法都**高估约 10 个百分点**。
门控 (iii) 的判定不变——它比的是与 released 行的配对差，不是与 1.0 的距离。

---

## 表示天花板参照行：把已完成的只读探索提升为注册工件（2026-08-21，用户批准）

追加节，不改上文。本节延续 2026-08-20「结果订正」小节封存的 438 协议真值参照行，
在同一协议、同一探针层级上再加一行：**表示天花板行**——真值本身被抽稀到网络实际预测的
stride-3 / 10 Hz 关键帧网格，再用评测器自己的插值函数还原回 126 帧后评分。

### 口径声明：本节是提升，不是发现

这些数**已经测过了**。读取路径 `.claude/scratch/repr-ceiling/`（`CEILING_TABLE.md`、
`ceiling_probe.py`、`ceiling_438.json`、`object_contact_ceiling.py`、`obj_contact_438.json`），
2026-08-20 的只读 CPU 探索。本次预注册做的是三件事，不含"重新发现"：

1. 把那些值提升为**注册工件**（registry 行 + 紧凑结果 + 可复算的探针），使 P13+ 能引用；
2. **补上探索没做的部分**——四个穿透项（天花板行**与**至今缺失的真值穿透地板），
   这一项直接关掉 P12 phase summary 的局限 #4；
3. 把探索**顺带发现但未注册的一个缺陷**写进记录：`code/utils.py:43` 的 lerp 权重互换。

**不得**把本节读成"重测确认了 08-20 的数"。除穿透四项外，其余数字与 `CEILING_TABLE.md` 同源，
若复算出现分歧，那是**两次测量至少一次错**，按停止分类 `repr-ceiling-contradicts-exploration`
处理，不许择一上报。

| 项 | 固定值 |
|---|---|
| 子阶段 | `1B-P12`（延续，不新开子阶段） |
| run id（拟） | `p1-hoi-p12-repr-ceiling-row-s42-20260821` |
| 类型 | **只读 CPU 探针**：无训练、无模型推理、无 checkpoint 加载、无 GPU、无 run 目录内的写 |
| 协议 | 官方 438 序列，与全部既有 HOI 行同一入口（每序列 3 窗口 × 42 帧 = 126 帧） |
| seed | 42（探针无随机性，仅为与既有行同口径而声明） |
| 主机 | 权威主机 CPU；不占 worker，不与 HSIPrior 训练争 GPU |
| 源码改动 | **草稿写"无"；已被本节末「载体决定」小节推翻，见该节** |
| 参考成本 | 见本节末「成本（实测订正）」 |

### 构造（写死，不许在看到结果后改）

对 438 序列中的每一条，取该序列 3 个窗口的真值通道，构造两条 126 帧运动：

| 行 | 构造 |
|---|---|
| **真值地板行**（ROW-GT） | 原生 30 Hz 真值，逐位等于评测器的 `points_fk_all_gt_48`（原生 48 帧切 `[6:]`，每窗口 42 帧） |
| **天花板行**（ROW-CEILING） | 真值抽稀到网络实际预测的 stride-3 关键帧网格（每窗口 `[::3][2:]` = 14 个关键帧，全序列 42 个），再走**评测器自己的**还原路径 |

天花板行的还原路径**必须**与 `code/test_infbagel_hoi.py:161-179` 的模型路径逐函数相同：

- 根平移：`interpolate_joints(root_keyframes, scale=3)`——线性、`1/scale` 基准（即 2026-08-19
  修复后的基准，不是发布版的旧基准）；
- 22 个局部旋转：`quat_ik_torch` → `matrix_to_quaternion` → `interp_jrot(q, 3)`（slerp）
  → `quaternion_to_matrix`；
- FK：`quat_fk_torch`，配**逐被试刚性 rest 模板** `rest_human_offsets`（不是逐帧骨长）；
- 物体：`interp_object(trans, rot9, 3)`，rest 顶点由 `data/object/rest_object_geo/*.ply`
  经 `trimesh.load_mesh` + `zup_to_yup` 取得；
- 打分：评测器自己的 `determine_floor_height_and_contacts`、`compute_foot_sliding_for_smpl`、
  `compute_gt_difference`、`compute_hand_object_interaction`、`compute_collision`。

三条窗口的插值**跨窗口拼接后一次做完**（42 关键帧 → 126 帧），与评测器一致；分窗口做会在
窗口边界少两个插值段。

**物体几何取网格顶点，不取 1024 点 `.npy`。** 探索一开始取错了，是被 `gt_contact_percent`
锚点抓出来的（`code/test_infbagel_hoi.py:505-517` 载入的是 `.ply` 网格）。这条写进构造，
是因为它已经错过一次。

### 18 项指标在这个来回下的信息量：15 项有信息，3 项解析固定

| 类 | 指标 | 天花板行的行为 |
|---|---|---|
| **有信息（15）** | `foot_sliding`、`feet_height` | 抽稀-插值本身改变足部速度与地板估计 |
| | `mpjpe`、`trans_dist`、`obj_trans_dist`、`obj_rot_dist` | 对真值自比，非零残差**就是**表示往返误差 |
| | `contact_precision`、`contact_recall`、`contact_f1`、`contact_acc`、`contact_percent` | 插值改变手-物距离，接触标签随之改变 |
| | `hand_pen_loss_omomo`、`hand_pen_ratio`、`human_pen_loss_infbagel`、`human_pen_ratio` | **本次新增**：插值后的顶点与插值后的物体位姿一起进 SDF |
| **解析固定（3）** | `end_obj_trans_err`、`xy_points_err` | **恒为 0**：二者读的是**插值前**的关键帧通道（`obj_trans_pred_seg[-1:,-1,:]` 与 `points_orig_seg[:,-2,:]`），抽稀不动关键帧本身 |
| | `gt_contact_percent` | **恒为 0.6618830180474017**：只由真值通道决定，与预测侧任何变换无关 |

`pelvis_goal_error_cm` 是逐序列输出里的第 19 项，**不在这 18 项内**，本行不报、不作闸门。

### 携带进来的已测值（08-20 探索，本行的"待封存"内容）

| 指标 | 真值地板 | **天花板** | P12 模型行 | 表示侧占差距 |
|---|--:|--:|--:|--:|
| `foot_sliding` | 0.26346464114890705 | **0.30888668343082337** | 0.34159243 | **58.1%** |
| `feet_height` | 0.034326739609241486 | **0.03426290431047139** | 0.061095 | **−0.2%（无）** |
| `mpjpe` cm | 0 | **0.15603576321154833** | 11.2839 | 1.4% |
| `trans_dist` cm | 0 | **0.09827817557379603** | 7.9378 | 1.2% |
| `obj_trans_dist` cm | 0 | **0.11386023834347725** | 14.1053 | 0.8% |
| `obj_rot_dist` | 0 | **0.0055702434487986605** | 0.96753 | 0.6% |
| `contact_precision` | **0.906392694063927** | **0.9035295547070061** | 0.80944 | 3.0% |
| `contact_recall` | **0.906392694063927** | **0.9022804362750585** | 0.63314 | 1.5% |
| `contact_f1` | **0.906392694063927** | **0.9027963392207574** | 0.67565 | 1.6% |
| `contact_acc` | 1.0 | **0.9968833804450242** | 0.76410 | 1.3% |
| `contact_percent` | 0.6618830180474017 | **0.6607595854171195** | 0.50042 | 0.7% |
| `gt_contact_percent` | 0.6618830180474017 | 同值（不变量） | 同值 | — |
| `end_obj_trans_err` | 0 | 0（解析） | 2.7861 | 0% |
| `xy_points_err` | 0 | 0（解析） | 3.6187 | 0% |
| 四个穿透项 | **本次新增** | **本次新增** | 0.17201 / 0.13282 / 2.72824 / 0.13654 | 本次得出 |

接触三列以 **0.906392694063927** 为参照而不是 1.0，成因、41 条零真值接触序列的证据、
397 条有定义子集上的 cp 0.9968411711880822 / cr 0.9954630506006943 / cf 0.9960322331956971、
以及"32 条既无接触读数也无穿透读数"这条覆盖率事实，**已在本文件上一节（2026-08-21）、
`docs/HOIPRIOR_EVIDENCE_INDEX.md` 结论 20、`docs/phase_summaries/PHASE_1B_P12_REPRESENTATION_FRAME.md`
2026-08-21 追记里记全**，本节不复述、不作为第二次独立发现。用 1.0 作分母会把这三列读成
50.6% / 26.6% / 30.0%，那测的是退化序列的兜底值，不是表示。

**修复前（发布版）插值基准的同一协议对照**：`foot_sliding` = 0.34482367856102686，
**高于** released 行的 0.33336。即在发布版评测器下，**真值自己在 FS 上比发布模型还差**；
修复后天花板落到 0.30889，低于两个模型。这一条独立于任何模型，是 2026-08-19 插值基准
修复的直接后果。

### 正确性闸门（必须在本行 run 内重跑，不许引用 08-20 的通过记录）

| 闸门 | 要求 |
|---|---|
| A1 | 真值 `foot_sliding` 复现 **0.26346464114890705**（容差 1e-07，08-20 探针实测偏差 2.5e-08） |
| A2 | 真值 `feet_height` 复现 **0.034326739609241486**（容差 1e-08，实测 6.5e-10） |
| A3 | `gt_contact_percent` **逐位**复现 0.6618830180474017 |
| A4 | 零真值接触序列数 **恰为 41**；A4b：真值地板行自己的 `contact_f1` 恰为 397/438 = 0.906392694063927 |
| A5 | 穿透覆盖序列数 **恰为 181 / 438**（六个被排除物体类共 257 条） |
| A6 | `human_pen_loss_infbagel` 必须带 `× 10475 / 100` 缩放（`test_infbagel_hoi.py:287,364`），`hand_pen_loss_omomo` 不带 |
| A7 | SDF 出盒守卫：真值**穿透损失**两项必须严格为正且与 P12 模型行同量级（比值落在 [0.01, 100]）；若塌向 0，视为 1.87 m 顶点位移那一类失败，不得封存 |
| E1 | 复算值与 08-20 探索的对应已测值一致（相对 1e-05，绝对下限 1e-06）；超出即 `repr-ceiling-contradicts-exploration` |

A1–A3 是 08-20 已通过的锚点，**在本 run 内重跑**是因为本行要新增 SMPL-X 顶点与 SDF 两条
以前不在探针里的路径，任何共享上游（dataset 构造、FK、插值）的漂移都必须先被这三条抓住。

### 新增内容 1：四个穿透项，天花板行**与**真值穿透地板

这一项关掉 P12 phase summary 的局限 #4（"4 个穿透指标未测"）。所需资产：

- SMPL-X：`smpl_models/smplx` + 每序列 `betas`(16) / `gender` / `transl`(3)，
  经 `create_smplx_model` / `run_smplx_model`（`code/utils.py:303,326`）；
- 物体：`data/object/rest_object_sdf_256_npy_files/*.npy` + 同名 `.json`（centroid / extents），
  rest 网格 `rest_object_geo/*.ply`；
- 帧约定：`compute_collision` 的输入全部经 `yup_to_zup`（顶点、物体位姿），
  这是真实的坐标变换，不是 2026-08-19 被删掉的补偿性三明治，**保留**。

两行的物体位姿来源不同，这一点是承重的：穿透项的 SDF 坐标系**由物体位姿本身给出**
（`test_infbagel_hoi.py:277,281` 用的是与人体同侧的那份 `obj_rot_mat`/`obj_trans`），
所以**天花板行必须用往返后的物体位姿，真值地板行必须用原生真值位姿**。
把两者混用会得到一个既非地板也非天花板的量。

`code/test_infbagel_hoi.py:276` 硬编码排除 woodchair / whitechair / largebox / largetable /
plasticbox / trashcan 六类，故穿透四项**只覆盖 181 of 438**。本行照抄该排除，不修正它——
修正会使穿透列与全部既有 HOI 行不可比。

### 新增内容 2：`code/utils.py:43` 的 lerp 权重互换缺陷，**保留在天花板之内**

```
lerped = q1 * step + q2 * (1 - step)      # 发布版，权重互换
lerped = q1 * (1 - step) + q2 * step      # 正确写法
```

slerp 分支（`:41`）是对的，错的只有退化分支。该分支仅在 `dot > 1 - eps`（`eps = 1e-6`）时触发。

**实测（438 序列真实关键帧四元数）：** 解析上界半角 `acos(1−1e-6)` = **0.08103°**，
关节旋转全角（= 2×半角）= **0.16206°**；检查 395,076 个关键帧关节对，
lerp 分支实际触发 **38,211 = 9.672%**；触发处 q1→q2 最大夹角 0.15826°，
发布版 vs 正确写法的实测最大偏差 **0.15826°**。

三条读法必须一起说，少一条就会误导：

1. **"很少触发"是错的**——9.672% 的关键帧关节对走的是这个分支。
2. **上界是紧的且被取到**——`step = 0` 时互换后的权重返回 `q2` 而非 `q1`，
   偏差恰等于 q1→q2 全角，实测最大偏差与触发处最大夹角**逐位相同**。
3. **0.08103° 是半角上界，不是关节朝向误差的上界。** 关节朝向误差上界是 0.16206°。
   之前记为"≈0.08°"的说法在量的定义上偏小一倍。

**本行的注册探针必须保留该缺陷**，因为天花板要度量的是**生产路径**，而生产路径带着它。
修掉它并重测差值是另一个实验，不在本节范围内；本节只把它写死为"在天花板之内"，
使任何后续引用不会把这 0.16° 误算到模型头上。

### 新增内容 3（范围限制）：天花板框架**不迁移**到 CHOIS FID

同日一次 CPU 探针度量：**FID(还原后的真值, 真值) = 0.001415**（新网格）/
**0.002357**（旧网格），而 P12 与 released 的 FID 差距是 **1.078**——低三个数量级。

结论：**插值/抽稀阶段对 `foot_sliding` 是一阶因子（58.1%），对 FID 可忽略（约 0.13%）。**
P12 的 CHOIS 侧亏空（FID 2.307 vs 0.933）**不能**用表示往返解释，它仍然是一条独立线索。
本行不许被引用为"FID 差距的一部分来自表示"。

（该 FID 数由本节之外的一次度量提供，本行**不复算**它，也不为它背书；
注册时须补上产出它的工件路径，否则该条以"外部携带、未在本 run 内验证"标注。）

### 范围限制：`feet_height` 的三段分解，"100% 是模型"是错的

08-20 探索给出插值阶段对 `feet_height` 的贡献是 **−0.2%**（天花板 0.03426290431047139
低于真值地板 0.034326739609241486，即抽稀-插值几乎不动地板估计），当时据此写成
"P12 的 +2.68 cm 垂直偏置 100% 归模型"。**这一步推论是错的**，因为它漏掉了评测器侧
的第二个阶段：另一次 Stage A 分析在**同一 checkpoint 内**测得 step-0 窗口帧规则本身
就值 `feet_height` **+0.004589**（CONTROL 0.056507 vs PRIMARY 0.061095）。

| 阶段 | 对 `feet_height` 到真值地板的差距（0.026768）的贡献 |
|---|--:|
| 抽稀/插值（本行度量） | **≈ 0，符号为负（−0.238%）** |
| 评测器 step-0 帧规则 | **+0.004589 = 17.1%** |
| 模型 | **≈ 83.1%**（= 100% − 17.1% + 0.2%） |

正确表述是"插值 ≈0（且略为负）、评测器 step-0 帧规则 ≈17.1%、模型 ≈83.1%"，**不是**"100% 是模型"。
两个阶段都在评测器侧、都不是模型的错，但只有第一个被本行覆盖。

#### 追记：那 83.1% 内部是什么（2026-08-21，用户度量，本行不复算）

上表把 83.1% 留作"模型"而未拆开。用户在 P12+Arm B 封存导出的全部 438 序列上
另测了骨盆高度对配对真值的跟踪斜率：**首个生成帧 1.0245**（corr 0.9851，偏置 **+2.874 cm**），
到第 3 个窗口衰减到 **0.1865**，且该衰减在 D2-X、D2-AI 与 P12 无引导 CONTROL 上**完全一致**。
读法：垂直偏置是**近似恒定的 frame-0 偏移 + 一个逐窗口忘记被试的 rollout**，
**不是**信息缺失问题。探针路径 `.claude/scratch/vertical_tracking/tracking_slope_probe.py`。
本行与该结论的关系是单向的：本行 `feet_height` 天花板 0.034263 对真值地板 0.034327
（−0.2%）**证明还原路径在这一项上是清白的**，把解释压力全部留给模型侧；
本行不复算、不背书那组斜率数字。

### 本行**不**建立什么（预先声明，不等被引用错了再解释）

1. **只界定 stride 3 这一档的抽稀/插值阶段。** 它对另一个 stride、对密集预测、
   对"若网络直接输出 30 Hz 会怎样"**一个字都没说**。
2. **它是与 released 基线共享的。** 发布模型走的是同一条 16 帧 → 126 帧还原路径，
   所以这条天花板**不能解释任何模型之间的差距**。它只界定"两个模型共同缴的表示税"。
   把它当作 P12 相对 released 落后的解释，是把共有项误当差分项。
3. **对 `foot_sliding` / `feet_height` 它是参照点，不是上界。** 这两项是运动的内蕴量，
   地板高度由 `determine_floor_height_and_contacts` 用 DBSCAN 从运动自身估，
   所以一个模型**可以合法地**比往返后的真值分数更低（更"平滑"、更"贴地"）。
   本行不许被写成"模型不可能低于 0.30889"。
4. **不在同一平滑度基线上的比较照旧成立**：模型 126 帧是 3 帧线性/slerp 段，
   真值地板是原生 30 Hz。弦长插值只会**低估**滑动，故不偏袒真值；
   但不能靠"给真值也插值"消除——那正是天花板行做的事，它是第三行，不是替换真值行。
5. **不含 FID、不含 R-Precision、不含 FPS。** 见上一节：FID 上该框架不成立。
6. **穿透四项只覆盖 181 of 438。** 剩下 257 条在任何 HOI 行上都没有穿透读数；
   其中与零真值接触重叠的 32 条对"人-物交互"这一族指标完全无信息，却仍占 438 的分母
   （已记于本文件上一节）。本行只记录这个事实，不改分母。

### 停止分类

- `repr-ceiling-row-established`：A1–A7 与 E1 全部通过，15 项有信息量指标（含四个穿透项）
  全部产出并封存为 HOI 常驻表示天花板参照行，供 P13+ 引用；
- `repr-ceiling-penetration-partial`：A1–A4 通过但 A5/A6/A7 任一失败——
  **穿透四项判为未建立**，其余 11 项照常封存，穿透留待另一次预注册；
  局限 #4 保持打开，不许写成已关闭；
- `repr-ceiling-anchor-fail-stop`：A1/A2/A3/A4 任一不复现——探针与评测器不同源，
  **不封存任何数**，回到评测路径排查；
- `repr-ceiling-contradicts-exploration`：复算值与 `CEILING_TABLE.md` 的对应已测值
  超出 float32 噪声——两次测量至少一次错，**两者都保留、都不封存**，先解释差异再谈封存；
- `repr-ceiling-subset-smoke`：`--sequences < 438`，实现期冒烟，**任何情况下不可封存**。

### 是否需要源码改动：不需要（**草稿原文，已被下一小节推翻，保留不改**）

全部所需函数都可 import，无一需要新增或修改被跟踪源码：

- `code/utils.py`：`interpolate_joints`、`interp_jrot`、`interp_object`、
  `load_object_geometry_w_rest_geo`、`zup_to_yup`、`yup_to_zup`、
  `yup_to_zup_rotation_matrix`、`create_smplx_model`、`run_smplx_model`；
- `code/eval_metrics.py`：`determine_floor_height_and_contacts`、
  `compute_foot_sliding_for_smpl`、`compute_gt_difference`、
  `compute_hand_object_interaction`、`compute_collision`；
- `code/datasets/infbagel.py`：`InfBaGelDataset` 及其**绑定方法**
  `quat_ik_torch`(`:924`) / `quat_fk_torch`(`:942`)。

**一处必须改的是探针自己，不是仓库源码。** 08-20 的两个探索脚本各自**复制**了
`quat_ik` / `quat_fk` 的函数体，而非 import。注册版**必须改用绑定方法**
`ds.quat_ik_torch` / `ds.quat_fk_torch`。

草稿在此处列了两条可选载体（`tools/` 下的新文件 / 沿用 `.claude/scratch/` 内的未跟踪探针），
并推荐前者，等用户裁决。

### 载体决定（2026-08-21，用户裁决）——推翻上一小节的"不需要"

上一小节的结论"源码改动：不需要"**已被用户 2026-08-21 的裁决取代**，按本仓库
append-and-supersede 的惯例保留原文不改。**若与上文冲突，以本小节为准。**

- **裁决内容**：探针的载体是**被跟踪源码树内 `tools/` 下的一个新文件**，
  即上一小节所列的选项 (a)；被否掉的是选项 (b)（继续留在 `.claude/scratch/` 内）。
- **裁决人**：用户。理由记在此：本行会成为 P13+ 的常驻参照，可跟踪 + 可复算优于一次性脚本，
  而 08-20 那次把探针留在未跟踪目录**正是本次需要"提升"的原因**。
- **载体**：`tools/measure_hoi_repr_ceiling.py`。CPU-only、无 checkpoint、无模型、无 GPU、
  不写任何既有 run 目录；`--output` 拒绝覆盖已存在路径并走
  `tools/chois_evaluator.atomic_output`（与 `tools/run_chois_evaluator.py:1213-1215` 同一惯例）；
  模块级 `EXECUTION_CONTRACT` 把这些声明成可被测试机械断言的对象。
- **`docs/EXPERIMENT_CONVENTIONS.md` §1 的记账**：该节要求"`tools/` 下新增文件必须在预注册里
  记下理由"。理由即上述两条：泛用路径 `code/test_infbagel_hoi.py` 没有真值自比通路
  （`save_chois_eval_npz` 只导关节），且本行是常驻参照而非一次性诊断。
  §5 的"实验关闭即退役脚本"**不适用**：常驻参照行的复算脚本正是 §6 意义上的比较基准。
- **允许文件范围（据此更新，锁定）**：
  1. `docs/plan/PHASE_1B_HOI/07_REPRESENTATION_FRAME.md`（本节，追加）；
  2. `experiments/registry.jsonl`（一行假设，仅追加）；
  3. `tools/measure_hoi_repr_ceiling.py`（**新增**）；
  4. `tests/test_research_governance.py`（该工具的断言；按 §3 不新建以实验命名的测试文件）。
  除此之外**不动任何文件**——特别是不动 `code/` 下任何文件，`code/priors/core/` 更是
  冻结的跨分支契约，一个字节都不碰。

### A6 / A7 是 pilot 校准的闸门，不是盲设的（必须打折读）

如实记录，因为这是"知道测量结果之后才定下容差"的情形：

- **A6 与 A7 是在一次 12 序列 pilot 之后才写定的**，不是盲设。
- **pilot 显示的**：真值地板行的两个穿透**比值**项（`hand_pen_ratio` / `human_pen_ratio`）
  在这 12 条上**恰为 0.0**，而两个**损失**项非零（hand 0.0140、human_infbagel 0.2178），
  且都落在 P12 模型行的同量级内。
- **据此改了什么**：A7 从"比值必须非零"改为**只对两个穿透损失项设闸**
  （严格为正 + 与 P12 同量级），比值为 0 降级为 `warn` 而非 `fail`。
  机制上这才是"出盒守卫"要测的东西：查询点出盒时 `grid_sample` 的 border padding
  返回正距离，`min(·,0)` 恰为 0，塌的是**损失**；比值只是 4 cm 深度的帧计数，
  真值本来就可能一帧都不到那个深度。
- **438 序列的可上报 run 尚未执行。** A7 是一条用与可上报 run 同分布的 pilot 校准出来的闸门，
  因此它比盲设的闸门证据力弱，读者应据此打折。A1–A5 与 E1 不受影响：
  它们对的是 08-20 之前就封存的值与计数。

### 与本文件上一节的重叠：已压缩为指针（如实记录）

草稿写作时（38eb722 之前）把接触上限 0.906392694063927 的完整证据链、41 条零接触序列的
稳健性反证、以及"三处 1.000 是错值"的逐处清单都写进了本节。这些内容**在本节合并前已经
被记入本文件上一节（2026-08-21）、`docs/HOIPRIOR_EVIDENCE_INDEX.md` 结论 20 与
`docs/phase_summaries/PHASE_1B_P12_REPRESENTATION_FRAME.md` 的 2026-08-21 追记**，
`/data/yujinlun/report/baseline.md` 也已就地更正。按 `docs/EXPERIMENT_CONVENTIONS.md` §5
（叙述只写一次），本节把那部分**压缩为指针**，只保留本行自己新增的东西：
把上限做成 run 内闸门 A4/A4b，以及把 0.906392694063927 而非 1.0 用作占比表的分母。

同时订正草稿里一个较松的数：跨模型独立上界，草稿写"四模型交集 ≤ 44"，
本文件上一节给出的更紧结果是**42**（全部 47 个已封存 438 序列 run、24 个互异集合的交集，
比 41 多 `sub17_woodchair_029` 一条）。以 **42** 为准；本行仍取直接实测的 **41**。

### 成本（实测订正）

草稿把穿透阶段估为 5–20 min CPU、并预留"若超过 1 h 可改用 GPU"。**该估计偏高，已实测订正**：

| 阶段 | 实测 |
|---|---|
| dataset 构造（CPU，1314 窗口） | **4.9 s** |
| 12 序列全流程（含穿透，`OMP_NUM_THREADS=4`） | **10.1 s 打分 + 4.9 s 构造 = 18.3 s 总计** |
| 每序列（含穿透） | **≈ 0.84 s** |
| 438 序列外推（181 条走穿透，257 条不走） | **约 4–6 min CPU** |

因此**不需要**任何 GPU 回退路径，穿透阶段也不需要预留一小时。峰值内存由按需加载的
256³ SDF 决定：只有 7 个非排除物体类会被查询，约 0.5 GB，而不是草稿写的 2.6 GB。

### 已知局限

1. **穿透四项没有逐位正确性锚点。** `foot_sliding` / `feet_height` / `gt_contact_percent`
   各有一个封存值可对，穿透没有：模型侧的 16 帧关键帧输出**没有被保存**
   （只留了 `per_sequence_metrics.json` 与只含 `global_jpos` 的 CHOIS npz），
   所以探针无法用重算 P12 穿透值的方式自证。可得的最强代理只有 A5（覆盖数 181）、
   A6（缩放因子，且它只是接线检查）、A7（出盒守卫，且它是 pilot 校准的）。
   **这是本行最弱的一环。**
2. **`compute_foot_sliding_for_smpl` 原地修改输入且缺权重钳制**（低于地板的权重可达 2.0），
   故 FS 列有一部分在测穿透。对三行（真值、天花板、模型）同向，不影响相对读法。
   另有一条 CPU 专属陷阱：生产路径在 CUDA 上跑，`tensor.cpu().numpy()` 会另开主机缓冲，
   原地修改传不出去；**在 CPU 上 `.cpu()` 返回自身**，若不显式复制，接触与 MPJPE 会被
   悄悄拿去和"已减掉地板高度"的关节比。本行的探针对这两个调用各传一份副本。
3. **41 与 181 两个计数依赖 dataset 的确定性构造。** 本节实测用 `seed 42` 且在
   `ds[i]` 取用前后各重置一次 `numpy`/`torch` 随机源；若将来 dataset 的窗口切分改变，
   两个计数都必须重测。
4. **本行不解释、也不试图解释 CHOIS 侧亏空。** 见"新增内容 3"。
5. **`feet_height` 的三段分解里，17.1% 那一段与骨盆跟踪斜率那一段都来自本行之外的度量**，
   本行只引用其结论、不复算其数。

### 不做什么

- **不改任何被跟踪文件里的既有数。** 三处 1.000 的更正**已由 38eb722 一线的追加节完成**，
  本节不再重复，也不改封存的紧凑结果（逐字节保留）。
- **不改 `code/eval_metrics.py:309-323` 的退化分支。** 改它会作废全部既有接触列。
  解析上限是要**被记录**的事实，不是要被修掉的 bug。
- **不修 `code/utils.py:43`。** 见"新增内容 2"：注册探针保留它以匹配生产路径。
- **不改穿透的六类排除、不改 438 的分母。**
- **不为让检查通过而改测试或 validator。**

### 结果（2026-08-21 实测，`p1-hoi-p12-repr-ceiling-row-s42-20260821`）

分类 **`repr-ceiling-row-established`**。**A1–A7 七个正确性闸门全部通过**，全部携带进来的锚点复现：
`gt_foot_sliding` 差 2.5484e-08、`gt_feet_height` 差 6.5490e-10、`gt_contact_percent` 差
**恰好 0.0**、零接触序列 41、穿透覆盖 181、解析上限 0.906392694063927。
因此**没有** `repr-ceiling-contradicts-exploration` 事件。
CPU 220.7 s（dataset 4.9 + scoring 212.5），远低于"5–20 分钟"的预估。
provenance 记录 `git_commit c05aca94591628a44f246c2a46d5ba5a88dbe6ee`、`git_dirty false`。

#### 新结果：四个穿透项有一个非零的真值地板，且约为模型值的一半

| 项 | 真值地板 | 天花板 | P12 模型 | P12 / 真值 |
|---|--:|--:|--:|--:|
| `hand_pen_ratio` | **0.064150** | 0.064544 | 0.13282 | **2.0705×** |
| `human_pen_ratio` | **0.065202** | 0.065728 | 0.13654 | **2.0941×** |
| `hand_pen_loss_omomo` | **0.083060** | 0.082905 | 0.17201 | **2.0709×** |
| `human_pen_loss_infbagel` | **1.299681** | 1.297220 | 2.72824 | **2.0992×** |

**真值自己就穿透。** 它是一个 SMPL-X 拟合，拿去和物体 SDF 打分，所以地板不是 0。
**本仓库此前把任何穿透数字读成"离 0 有多远"都是错的**：模型是可达地板的 **2.07–2.10×**，
不是"比 0 差无穷多"。四项的表示份额全部 **< 0.3%**，即 keyframe/interp/FK 往返对穿透几乎无贡献。
这关闭了 `docs/phase_summaries/PHASE_1B_P12_REPRESENTATION_FRAME.md` 的局限 #4。

#### 表示份额，逐指标

`foot_sliding` **58.1%** 是唯一被表示支配的指标。`feet_height` **−0.2%（无）**——
这正是证明第 0 帧竖直偏置属于模型、不属于往返路径的那一条。四个穿透项各 < 0.3%；
`mpjpe` 1.4%、`trans_dist` 1.2%、`obj_trans_dist` 0.8%、`obj_rot_dist` 0.6%；
`contact_precision`/`recall`/`f1` 为 3.0% / 1.5% / 1.6%，且是**对 397/438 解析上限**算的，不是对 1.0。
CHOIS FID 按上文明确排除在本框架之外（往返 FID 0.0014）。

**下一步**：把分支里每一处穿透对照都改成对实测地板读，而不是对 0 读。两条 live lead 不变——
第 0 帧刚体竖直偏置（P13 已标价为 D2-X 上 1.142 FID、D2-AI 上 1.054）与接触参与度。
