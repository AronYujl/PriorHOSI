# Phase 2.11 independent-development calibration — 2026-09-07

**Calibration complete; lambda26 passes the registered candidate constraints.**
The numerical scale is fixed from independent development sources. Scene-proxy
benefits remain small and contact-anchor drift increases. Full469-task native
quality and the contribution of HSI remain open.

## Scope, inputs and isolation

The user approved independent-development calibration after Phase2.10. Branch
`phase/02k-scene-evidence-calibration` retains frozen P15 online/Arm B and R2 final
EMA,500 diffusion steps,8 editor levels, beta_ref1, the complete explicit
objective,67D bounds and local Armijo settings. No checkpoint is trained or changed.
Full469 comparison moves to Phase2.12 / `phase/02l-scene-evidence-benchmark`.

The task manifest is
`experiments/tasks/scene_evidence_development_s42_20260907.json`. It pairs four
OMOMO INTERNAL-VALIDATION initial motions/texts from data/train with LINGO v3 TRAIN
scenes. Sources: clothesstand window1509/sequence16, floorlamp5507/51,
largebox10079/91 and suitcase236335/1844. They include pull, lift, push and kick/lift
instructions. For each object the first pi0 language window in the internal
validation partition is selected. No official HOI/HOSI test motion selects lambda.

Numeric base LINGO scenes with an existing training episode chord in[.4,2.0]m
are ordered by seed42 permutation; seven qualify. Calibration uses004/006/055;
verification uses023/037/036;067 stays unused. Each scene uses its first eligible
existing LINGO training path. Object goal preserves the initial HOI object height
and adds its heading-rotated root-object xz offset to the LINGO pelvis endpoint.
The HOI source is generated by the existing sampler using its actual fixed history.
These independent pairings provide no guarantee of initial object-scene feasibility;
source collisions and exterior points are retained in the measurements.

All six families belong to the fixed LINGO training split and are disjoint across
calibration/verification. Scene IDs are disjoint from the67 HOSI test scenes.
GPU7 compared each selected full occupancy array with every benchmark grid:
**402 exact comparisons, zero duplicates**. No hash mechanism was added. The
input directory links existing train motion assets and the six original LINGO
occupancy grids, used read-only. The builder, manifests and links are preserved at
`results/evidence-scale-inputs-s42-20260907/`. This comparison proves grid identity
exclusion; it does not perform geometric-isomorphism matching across datasets.

## Correctness repair and implementation

The actual `Sampler._compute_occ_sample` samples object vertices with CPU
`torch.randperm`. Phase2.10's teacher called that function without isolating its
ambient random stream. The scene teacher now carries a separate per-window CPU
generator through a forked CPU/CUDA RNG context. Source/candidate diffusion noise
and known-empty modalities keep their existing separate streams. Tests execute
the native five-query occupancy path, confirm unchanged ambient CPU/CUDA RNG and
identical sampled vertices despite unrelated ambient draws. Historical2.10
outputs remain immutable; its teacher recipe differs from this corrected one.

`SceneEvidenceEditor(mode=calibrate)` queries all eight levels at the fixed
reconstructed source, computes parameter gradients and returns the raw source
exactly. It performs no optimization. `mixer/scene_calibration.py` contains the
named global-scale estimator and development episode recorder. Existing evaluator
configuration can now select a task directory. Its development-output mode reuses
original generation, A*, world transformation and history rebase, then writes
window proxies and motions before native mesh/SDF evaluation. These LINGO scenes
lack the native HOSI mesh-SDF bundle. The run explicitly records
`quality_evaluated=false`; it supplies no native FS/completion/penetration claims.
Default native evaluator behavior and its metrics remain intact.

One config fragment, `config_sample_hosi_evidence_scale.yaml`, inherits the
editor config and selects the independent task snapshot. No core, expert, native
metric or tools implementation changed. Registered source and contact/stance
masks stay fixed within each window. Every optimized term, source/final proxy,
boundary count and line-search decision is logged.

## Estimator and selected coefficient

On positive-scene-energy source windows, take the median over eight levels of
||g_explicit|| / ||g_HSI at lambda1||. Take the median per calibration scene, then
the median across the three scenes. Multiply by the preregistered target.1 and
round to two significant figures. This sets a single global coefficient without
normalizing weak signals per window or selecting values on verification outcomes.

| Calibration scene | Active windows | Median unit gradient ratio |
|---|---:|---:|
|004|8|549.2262|
|006|10|258.2077|
|055|8|183.0707|

The26 active windows meet coverage. Two zero-scene windows are retained but do not
enter estimation; no active query has zero HSI signal. The unrounded coefficient
is25.8207661; **lambda_dp=26** is locked. No alternative lambda was tried. On
verification sources, the scaled HSI/explicit gradient ratio median across312
positive-gradient level queries is.055216. That diagnostic was recorded after
selection and did not change the coefficient.

## Development verification

Stage1:24 passive tasks/68 windows on six scenes. Stage2:12 verification tasks
run at lambda0 and lambda26,40 windows per row. Total48 executions/148 windows
on24 unique scene-object tasks. All twelve GPU jobs exit zero. The global
coefficient remains26 throughout every verification task.

The following are per-task means of window FK/voxel proxies, in centimetres:

| Proxy | lambda0 | lambda26 | Delta |
|---|---:|---:|---:|
| human-scene nearest-free residual |4.861090|4.859632|-.001458|
| object-scene nearest-free residual |7.642006|7.637017|-.004989|
| hand contact-anchor drift |.136426|.177683|+.041257|
| source stance correction increment |.205030|.222867|+.017837|
| source root endpoint shift |.501101|.509934|+.008833|
| source object endpoint shift |.529888|.553489|+.023601|

Human/object scene-proxy point improvements are.0300%/.0653%. Human-scene delta
has nominal95% paired task interval[-.006122,.003801] and scene interval
[-.004359,.003552]. Object-scene intervals are[-.010437,-.000305] and
[-.006508,-.003177]. Contact drift increases at both units: task[.017416,.068489],
scene[.016521,.087375]cm. Object endpoint shift also increases at both units;
stance increment increases at task level with unresolved scene interval. All
source/final terms, proxy metrics and both12-task/3-scene paired reports use
10,000 seed42 replicates and are retained without metric selection. Three-scene
development uncertainty does not establish benchmark-wide quality.

The registered candidate gate passes: contact-anchor drift and stance increment
means remain below.5cm; both scene residual means remain below1.10x lambda0;
at least one update is accepted. This gate admits a numerically calibrated
candidate and does not require significant improvement or certify physical realism.
The small benefit and measured relation drift remain part of the result.

## Execution, checks and artifacts

All68 passive windows return their original sources exactly, with zero accepted
updates. The verification rows accept171/320 and190/320 updates. Lambda0 has141
search exhaustions and8 zero gradients; lambda26 has130 search exhaustions.
There are2056/1966 line-search trials and2302 domain-rejected trials combined.
All accepted trials satisfy registered local Armijo decrease; saved tensors are
finite; history/contact are exact; domain exterior counts/distances never rise.
World-history continuity error is at most7.15e-7 for measurement and5.96e-7 for
verification. Preserve source collision activity when interpreting these results.

Controller wall408.50s; manifest wall411s. Peak allocation504,060,928 bytes
(480.71MiB), below20GiB headroom. Verification generation sums129.50/137.96s;
editor sums57.46/66.47s; teacher sums2.27/13.97s and solver/gradient diagnostics
50.28/47.72s. The calibrated row adds640 raw HSI forwards to640 HOI reference
forwards. Each row saves12 motion files totaling2,643,340 bytes. Timings are
synchronized, instrumented, concurrent workload costs. The first stage paid
substantial full-training-dataset initialization cost; no extra performance or
smoke workload was added.

Implementation verification: **949 passed,4 skipped,164.69s**. The17 editor
component cases include the actual CPU/CUDA query RNG regression, passive
identity and scene-balanced estimation excluding verification/inactive records.
Four skips are existing historical HSI checkpoint/P8 artifact cases. All paired
verification configs differ only in lambda and output directory. Completion verification: **949 passed,4 skipped,178.79s**; the same two HSI
checkpoint-pair and two P8 artifact cases are skipped. Registry validation passes
with360 records and diff check passes. The selected benchmark configuration also
resolves successfully; it is archived as `resolved/benchmark-candidate-inspection.yaml`.

- Compact: `experiments/results/p2_mixer_evidence_scale_s42_20260907.json`.
- Run: `results/experiments/p2-mixer-evidence-scale-s42-20260907/`.
- Preregistration6376d68; implementation and complete run source4a4f9f5. Manifest
  start/finish use that same implementation source. No operational failure,
  restart, result overwrite or unregistered retry occurred.
- Preserve manifest, both execution plans, all12 resolved configs, config comparison,
  input manifests/builder, machine preflight, grid comparison, logs, selected_scale,
  both audits, full per-episode source/reference/output motions, every proxy/trace
  and both paired reports. Unchanged checkpoint/input identities are referenced
  through the recorded prior manifests and split manifests.

## Exact next entry and frozen settings

Read this summary, OVERVIEW.md and the Phase2.11 section of PHASE_2_COMPOSITION.md.
Retain lambda26, beta_ref1, levels300/264/229/193/157/121/86/50,8 iterations,
unchanged bounds/explicit terms/local Armijo and isolated teacher query RNG.
The comparison remains an identical lambda0 editor; retain native disabled HOI
and reconstruction controls. The development config is for measurement, not the
469 benchmark; use `config_sample_hosi_scene_edit` with lambda26 for the latter.

The exact completed verification commands are archived in
`verification_execution_plan.json`. The run id is consumed. For read-only config
inspection of the selected benchmark candidate:

```bash
export ROOT_DIR=/data/yujinlun/InfBaGel-mixer
export INFBAGEL_PYTHON=/data/yujinlun/anaconda3/envs/infbagel/bin/python
cd "$ROOT_DIR/code"
"$INFBAGEL_PYTHON" test_infbagel_hosi.py --config-name config_sample_hosi_scene_edit sampler.pelvis.scene_editor.lambda_dp=26 --cfg job --resolve
```

Phase2.12 on `phase/02l-scene-evidence-benchmark` preregisters full469-task native
metrics and scene/task uncertainty using these frozen settings. Use the existing
sharded evaluator/merge and paired-bootstrap tools, fresh run/output IDs and
archived resolved configs. No further lambda search uses the469 test set. Keep
paper InfBaGel aggregates separate from locally pairable baselines. This session
closes only2.11 and tags `exp/p2k-scene-evidence-calibration-v1`; full native quality,
HSI contribution and the wider Phase2 gate remain open.
