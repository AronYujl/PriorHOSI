defaults: [config_sample_hosi_multitask, _self_]

exp_name: p5-inference-handoff-audit-s42-20260910
device: cuda:0
multitask:
  stage: handoff_audit
  output_dir: ${oc.env:ROOT_DIR}/results/experiments/${exp_name}/handoff
  handoff:
    task_manifest: ${oc.env:ROOT_DIR}/experiments/tasks/p5_multitask_pilot_s42_20260909.json
    source_motion: ${stand_wait.source_root}/lanes/lane-07/task-027/motion.pt
    source_metrics: ${stand_wait.source_root}/lanes/lane-07/task-027/metrics.json
    source_arm: ${stand_wait.source_arm}
    goal_tolerance_m: 0.10
