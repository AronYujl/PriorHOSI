# Phase 2.5: inference-identical floor-free A00/A01 with motion observations.
defaults: [config_sample_hosi_floor_free_factorial, _self_]

sampler:
  pelvis:
    relational_corrector:
      record_motion: true
