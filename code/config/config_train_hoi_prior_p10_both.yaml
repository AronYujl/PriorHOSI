# p10_both: the sealed D2-AI recipe with the manipulated factor below.
#
# Rewritten mechanically on 2026-08-11 onto `recipe/d2ai`. The pre-refactor file
# restated the whole recipe inline; this file states only what makes this arm
# different. Proven equivalent: the fully-resolved config is identical key for
# key to the pre-refactor version (see the phase note for the check).

defaults:
  - config_train_hoi_prior
  - recipe: d2ai
  - _self_

run_id: "p1-hoi-p10-geom-both-s42-20260809"
exp_name: "p1-hoi-p10-geom-both-s42-20260809"
subphase: "1B-P10"

# Manipulated factor(s) and arm-specific deviations from the recipe.
hand_object_contact_detach_object: true
hand_object_contact_hinge: 0.02
hand_object_contact_weight: 3.0
