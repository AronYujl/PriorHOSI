# Phase 2.14a: saved-source replay and matched-proxy relation projection gate.
defaults: [config_sample_hosi_evidence_views, _self_]

hosi_replay_source: ${oc.env:ROOT_DIR}/results/experiments/p2-mixer-evidence-views-r1-s42-20260907
sampler:
  pelvis:
    scene_editor:
      diagnostics:
        probe: relation_compatible
        minimum_keep_ratio: 0.001
