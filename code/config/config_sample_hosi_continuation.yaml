# Paired outcome diagnostic; explicitly enabled through continuation.enabled.
defaults: [config_sample_hosi_candidate_pool, _self_]

continuation:
  enabled: false
  protocol: ${oc.env:ROOT_DIR}/experiments/protocols/p2_continuation_outcomes_s42_20260907.json
  scene: null
  state_ids: null
  resume_source: null
  reuse_states: []

# Explicitly declared for Hydra struct; standard loader default stays true.
dataset:
  load_object_payload: true
