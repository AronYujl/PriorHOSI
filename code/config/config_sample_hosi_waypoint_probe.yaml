# First-window control probe; generation and editor recipes inherit sealed B1.
defaults: [config_sample_hosi_candidate_pool, _self_]

waypoint_probe:
  enabled: false
  protocol: experiments/protocols/p2_waypoint_control_s42_20260907.json
  offset_m: 0.1
  first_window_only: true
  endpoint_mode: native_retained_v2
