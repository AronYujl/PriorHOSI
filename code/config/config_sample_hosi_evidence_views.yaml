# Phase 2.13: fixed reconstructed sources, paired temporal observations.
defaults: [config_sample_hosi_evidence_scale, _self_]

sampler:
  pelvis:
    scene_editor:
      mode: calibrate
      lambda_dp: 26.0
      teacher_scene_view: legacy_occupied
      diagnostics:
        enabled: true
        noise_draws: 3
        mismatch_local_x_m: 2.0
        physical_probe_rms_m: [0.001, 0.005]
