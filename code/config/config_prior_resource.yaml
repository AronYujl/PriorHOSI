expert: hoi
seed: 42
repo_root: ${oc.env:ROOT_DIR}
device: cuda
num_gpus: 8
batch_size: 128
effective_batch_size: 1024
gradient_accumulation_steps: 1
num_workers: 2
dataset_limit: 0
partition: train
optimizer_updates: 1
learning_rate: 0.0001
amp: true
window_frames: 16
history_frames: 2
diffusion_steps: 500
dim_model: 512
num_heads: 16
num_layers: 8
scene_grid_size: 32
init_checkpoint: null
metrics_path: null

hydra:
  run:
    dir: ${oc.env:ROOT_DIR}/results/outputs/prior-resource
