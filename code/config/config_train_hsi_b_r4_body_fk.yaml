# R4: preserve R2 and supervise GT body21 positions on every future frame.
defaults: [config_train_hsi_b_r2_fullbody_seam, _self_]

exp_name: hsi_b_r4_body_fk
# Calibration-only value; replace with the registered measured coefficient
# before the resource measurement or formal training.
body_fk_loss_weight: 1.0
body_fk_calibration: false
ckpt_interval: 1
log_grad_norm: true
diagnostic_checkpoint_epochs: [19]
