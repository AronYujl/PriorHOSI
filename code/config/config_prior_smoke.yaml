expert: hoi
seed: 42
repo_root: ${oc.env:ROOT_DIR}
device: cuda
num_gpus: 1
batch_size: 2
effective_batch_size: 2
gradient_accumulation_steps: 1
num_workers: 0
dataset_limit: 2048
partition: train
optimizer_updates: 1
learning_rate: 0.0001
window_frames: 16
history_frames: 2
diffusion_steps: 500
dim_model: 256
num_heads: 8
num_layers: 4
init_checkpoint: null
metrics_path: null

hydra:
  run:
    dir: ${oc.env:ROOT_DIR}/results/outputs/prior-smoke
