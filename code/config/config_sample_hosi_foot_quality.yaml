# Phase2.16: only the repair-local foot acceptance predicate changes.
defaults: [config_sample_hosi_conditional_repair, _self_]

sampler:
  pelvis:
    scene_editor:
      foot_guard_mode: quality
      foot_energy_epsilon_m2: 1.0e-12
