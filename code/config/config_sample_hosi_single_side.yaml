# Per-window candidate decisions; frozen B1 and scores inherited by reference.
defaults: [config_sample_hosi_candidate_pool, _self_]

sampler:
  pelvis:
    _target_: mixer.single_side_waypoint.SingleSideWaypointSampler
    waypoint_policy: off
    waypoint_protocol: ${oc.env:ROOT_DIR}/experiments/protocols/p2_single_side_waypoint_s42_20260907.json
