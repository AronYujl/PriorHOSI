"""HOIPrior expert package: scene-free object-interaction prior.

Owned by ``phase/01b-hoi``.  Free to evolve independently of ``priors.hsi`` as
long as the ``priors.core`` contract is untouched.  Deliberately re-exports
nothing so importing one module never drags in the rest.
"""
