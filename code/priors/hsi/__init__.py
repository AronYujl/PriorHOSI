"""HSIPrior expert package: scene-conditioned prior.

Mirrored read-only on the HOI branch; ``phase/01c-hsi`` owns it and recombination
is a directory graft, not a git merge.  Deliberately re-exports nothing.
"""
